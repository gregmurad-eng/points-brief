# Repo notes for Claude sessions

Publishing repository for a daily newsletter: one dated `YYYY-MM-DD.html`
per issue; `index.html` is the latest. Published artifacts only — generation
and indexing happen in private infrastructure, not here.

- No build step, no server, no dependencies.
- Never add keys, tokens, or personal data to this repo.
